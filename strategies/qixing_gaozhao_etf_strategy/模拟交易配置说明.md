# 七星高照ETF轮动策略 · 模拟交易配置说明

> 策略注册名：`qixing_gaozhao`
> 适用模式：`sim`（模拟盘）、`instances`（多实例常驻）
> 定位：纯行情 ETF 轮动（A 组，无财务依赖），**每日调仓**

---

## 1. 策略速览

| 项 | 内容 |
|----|------|
| 标的池 | 46 只 ETF（自持固定池，见 [config.py](config.py)）+ 防御货币 ETF 511880 |
| 调仓频率 | 每日（每个 `on_bar` 评估动量排名并调仓） |
| 持仓 | 前 5 只合格 ETF 等权，不足用 511880 补位 |
| 数据需求 | 仅行情（无财务），**无需 `--pool`** |
| T+0 | 全部 ETF 已设 T+0（可当日买卖） |
| 收盘退出 | 15:05 自动退出（sim/real 单策略模式） |

---

## 2. 环境准备

### 2.1 前置依赖

| 组件 | 要求 | 说明 |
|------|------|------|
| QMT 客户端 | 模拟交易端，登录并进入 MiniQMT 模式 | 记录 `userdata_mini` 目录 |
| XtQuant | `pip install xtquant`（随 QMT 提供） | 下单/行情主推 |
| Python 环境 | 项目内 `.venv/` | 依赖见 `requirements.txt` |

```powershell
# 验证 xtquant 可用
.venv\Scripts\python.exe -u -c "from xtquant import xtdata; print('xtquant OK')"

# 验证策略可被发现
.venv\Scripts\python.exe -u -c "from strategies import get_strategy; print(get_strategy('qixing_gaozhao') is not None)"
```

### 2.2 QMT 模拟端

- **务必先连「模拟交易端」而非实盘端**：本策略下单走 MiniQMT 真实下单通道，模拟盘与实盘的本质差异只在 `--qmt-path` 指向哪个端的 `userdata_mini` 目录。
- 典型模拟端目录示例：`D:\国金QMT交易端模拟New\userdata_mini`

---

## 3. 启动方式

### 3.1 单策略模拟盘（推荐首次验证）

```powershell
.venv\Scripts\python.exe -u main.py --mode sim --strategy qixing_gaozhao --qmt-path "D:\国金QMT交易端模拟New\userdata_mini" --account 80000123 --debug
```

- 当日进程：交易日 9:30-15:00 运行，15:05 自动退出
- 无 `--account` 时自动取第一个资金账号；取不到会报 `未找到账户`，此时需显式传 `--account`

### 3.2 多实例常驻（instances 模式）

适合与其它策略同账户/多账户并行。先建 `config/instances_sim_config.json`（目录被 gitignore，需本机自建）：

```json
{
  "instances": [
    {
      "instance_id": "qixing_gaozhao_sim",
      "strategy_name": "qixing_gaozhao",
      "mode": "sim",
      "account_id": "80000123",
      "initial_capital": 100000,
      "claim_existing_positions": true,
      "cash_ratio": 1.0,
      "kwargs": {}
    }
  ]
}
```

```powershell
.venv\Scripts\python.exe -u main.py --mode instances --instances config\instances_sim_config.json --debug
```

> instances 模式**无收盘自动退出**，需在 5 秒内连按两次 Ctrl+C 退出。

---

## 4. 配置参数说明

### 4.1 策略参数（默认值）

| 参数 | 默认 | 说明 |
|------|------|------|
| `holdings_num` | 5 | 持仓 ETF 数量 |
| `lookback_days` | 60 | 动量计算周期 |
| `defensive_etf` | `511880.SH` | 防御货币 ETF |
| `liquidity_min_amount` | 1e8 | 日均成交额门槛（元） |
| `liquidity_lookback` | 20 | 流动性回看天数 |
| `stop_loss` | 0.92 | 固定止损线（跌 8% 触发） |
| `loss` | 0.965 | 近 3 日单日跌幅过滤（-3.5%） |
| `enable_volume_check` | True | 高位放量过滤开关 |
| `volume_threshold` | 2.5 | 量比阈值 |
| `volume_return_limit` | 1.0 | 放量过滤年化收益上限 |
| `use_r2_filter` | True | R² 过滤开关 |
| `r2_min_threshold` | 0.4 | R² 最低阈值 |
| `min_score_threshold` | 0.0 | 动量得分下限 |
| `max_score_threshold` | 5.0 | 动量得分上限 |
| `rebalance_threshold` | 0.05 | 调仓容差（偏离 5% 内不调） |

### 4.2 运行时覆盖参数

```powershell
# 单策略：--strategy-params 覆盖
--strategy-params "stop_loss=0.90,holdings_num=8"

# instances：kwargs 字段覆盖
"kwargs": {"holdings_num": 8, "stop_loss": 0.90}
```

### 4.3 交易成本口径（回测已配，模拟盘实际以成交回报为准）

| 项 | 回测值 |
|----|--------|
| 佣金 | 万 2（最低 5 元） |
| 印花税 | 0（ETF 免征） |
| 滑点 | 未显式建模 |

---

## 5. 标的池与数据

- **自持固定池**：46 只 ETF 全部写在 `config.py` 的 `ETF_POOL`，运行时不依赖 `--pool` 或成分股下载。
- **无财务依赖**：本策略只用行情数据（收盘价、成交量），不需预下载财务数据。
- **首次运行建议**：若 QMT 尚未缓存这些 ETF 的历史行情，可先运行一次回测或数据种子脚本预热缓存：

```powershell
# 预热 ETF 行情缓存（QMT 在线时自动下载）
.venv\Scripts\python.exe -u strategies\qixing_gaozhao_etf_strategy\run_qixing_gaozhao_backtest.py 2025-01-01 2025-12-31 --headless
```

---

## 6. 首日验证清单

启动后按时间点核对：

| 时间 | 检查项 |
|------|--------|
| 09:25 | 启动日志无 ERROR；持仓认领与账户一致 |
| 09:35 | 首笔 `on_bar` 触发；出现「动量排名前5」info 日志 |
| 09:45 | 若当日有信号，核对委托/成交回报与排名一致 |
| 盘中 | 抽看 VirtualBook 持仓与模拟账户持仓一致 |
| 15:00 | 收盘清算；15:05 自动退出干净 |
| 盘后 | 核对 `trading_records/qixing_gaozhao_sim/trades_*.csv` 与账户流水 |

---

## 7. 注意事项

1. **ETF 为 T+0**：策略已设 `set_t_plus_1(symbol, False)`，当日可买卖，无需 T+1 等待。
2. **QDII 溢价扰动**：跨境 ETF 原版用基金净值算动量，本项目 QMT 数据源无净值接口，回退用市场收盘价，模拟盘信号可能略受溢价影响。
3. **止损粒度**：原版盘中每分钟止损，本项目日线框架下改为每日 `on_bar` 开盘检查一次；盘中急跌无法即时离场，需人工关注。
4. **每日调仓**：换手较频繁，模拟盘阶段重点观察手续费磨损与信号漂移。
5. **15:05 自动退出**：单策略 `sim` 模式跑完当日即退出，需每日启动（可配合 Windows 计划任务 9:15 自动拉起）。

---

## 8. 常见问题

| 现象 | 原因 | 解决 |
|------|------|------|
| `QMT未连接且本地缓存未完整覆盖` | QMT 未启动/未登录 | 启动并登录 QMT MiniQMT，或先预热缓存 |
| `未找到账户` | 未自动取到资金账号 | 显式传 `--account` |
| `交易接口未初始化`（下单返回 None） | xtquant 缺失 | 安装 xtquant，检查 QMT 登录 |
| `自动发现策略模块失败: ... No module named 'scipy'` | 同包下其它策略缺 scipy | 非致命告警，不影响本策略；可安装 scipy 消除 |
| 双击 `start_sim_now.bat` 失败 | bat 内 `cd` 到不存在的旧目录 | 改用上方命令行，或先修 bat 路径 |

---

## 9. 相关文档

| 文档 | 内容 |
|------|------|
| [readme.md](readme.md) | 策略逻辑、参数、回测结果 |
| [optimization/优化记录.md](optimization/优化记录.md) | 优化历程与后续方向 |
| [optimization/2025年度回测报告.md](optimization/2025年度回测报告.md) | 2025 年度专业回测报告 |
| [../../docs/live-deployment.md](../../docs/live-deployment.md) | 全策略实盘部署总指南（环境/回滚/FAQ） |
| [../../docs/usage.md](../../docs/usage.md) | 回测/模拟/实盘/多实例用法 |
