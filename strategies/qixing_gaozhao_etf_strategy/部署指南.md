# 七星高照ETF轮动策略 - 实盘部署指南

> 适用策略：`qixing_gaozhao`（七星高照ETF轮动策略，优化版）
> 目标：从回测 → 模拟交易 → 实盘交易，逐步验证上线

---

## 1. 部署流程总览

```
回测验证（已完成） → 模拟盘验证（1-4周） → 小资金实盘 → 常规运行与监控
     ✅                     ⬅ 必须步骤                 ⬅ 建议首次实盘使用
```

框架核心目标：**一套策略代码无缝切换回测/模拟/实盘**，部署时不需要修改策略代码，仅切换运行命令。

---

## 2. 前置条件

### 2.1 环境依赖

| 依赖 | 说明 |
|------|------|
| Python 环境 | 项目虚拟环境 `.venv/`（已安装 `requirements.txt`） |
| QMT 客户端 | 券商版 QMT，已登录，勾选 **MiniQMT（极简模式/独立交易）** |
| XtQuant 库 | QMT 配套，已安装并能 `import xtquant` |
| 数据源 | 策略行情依赖 QMT（`data_source=qmt` 默认） |

### 2.2 确认 QMT 环境（关键）

运行前确认 QMT 客户端已启动并登录，否则策略无法获取行情/下单。可快速验证：

```bash
.venv\Scripts\python.exe -u -c "from core.data.qmt import QMTDataProcessor; p = QMTDataProcessor(); print('connected:', p.check_connection())"
```

输出 `connected: True` 表示 QMT 连接正常。

> 本机参考路径：`D:\国金QMT交易端模拟New\userdata_mini`（以实际安装为准，
> 通过 `--qmt-path` 指定）。

### 2.3 策略数据说明

- 策略交易 46 只 ETF（含货币 ETF 511880 防御仓），代码为 `.SH`/`.SZ` 格式（QMT 标准格式）
- QMT 连接时行情按需自动下载；首次运行会较慢属正常
- ETF 为场内品种，**T+0**（代码中已 `set_t_plus_1(..., False)`）

---

## 3. 第一步：模拟盘验证（必须）

模拟盘用于验证**策略信号、下单链路、持仓簿记**是否与回测一致，不涉及真实资金。

### 3.1 启动模拟盘

```bash
# 方法一：脚本式（自动发现策略参数）
.venv\Scripts\python.exe -u main.py --mode sim --strategy qixing_gaozhao ^
    --qmt-path "D:\国金QMT交易端模拟New\userdata_mini" --debug
```

> PowerShell 中续行符为反引号 `` ` ``，或用单行：
> `python main.py --mode sim --strategy qixing_gaozhao --qmt-path "D:\国金QMT交易端模拟New\userdata_mini" --debug`

程序将持续运行至当日收盘（15:05）或 Ctrl+C 停止，行情驱动策略每个交易日执行调仓。

### 3.2 模拟盘验证清单

| 检查项 | 通过标准 |
|--------|---------|
| 行情接入 | 日志无持续 `QMT未连接` 报错 |
| 动量排名 | 每天日志输出「动量排名前5」（info 级） |
| 买入/卖出 | 日志出现买入/卖出成交，数量为 100 股整数倍 |
| 防御补位 | 强势标的不足 5 只时买入 511880 货币 ETF |
| 止损触发 | 行情下跌时触发止损卖出（可临时调低 `stop_loss` 验证） |
| 虚拟簿记 | 模拟账户持仓与日志一致 |

### 3.3 推荐模拟周期

**至少连续 1-4 周**（含不同类型行情），对照回测口径观察：

- 单日动量与回测窗口信号差异（< 5% 为正常数据延迟差异）
- 交易成本：模拟盘采用与实盘一致的佣金/滑点口径（万 2，最低 5 元）
- 若模拟期间出现异常信号或频繁止损，先回到回测定位原因，**不要带病上实盘**

---

## 4. 第二步：实盘部署

### 4.1 启动实盘

```bash
# 建议：首次先小资金（如 10 万元起）
.venv\Scripts\python.exe -u main.py --mode real --strategy qixing_gaozhao ^
    --qmt-path "D:\国金QMT交易端模拟New\userdata_mini" --account <资金账号> --debug
```

参数说明：
- `--account`：QMT 资金账号；不传则自动获取第一个
- `--debug`：输出 DEBUG 级日志便于排查（日常运行可去掉）

### 4.2 实盘关键注意

| 事项 | 说明 |
|------|------|
| 下单模式 | 框架默认按 **限价单（FIX_PRICE）** 下单，价格取行情最新价；成交价与信号价存在滑点，策略回测已按近似口径验证 |
| T+0 | ETF 当日可买卖，策略每日调仓不受 T+1 限制 |
| 停牌/涨跌停 | 框架自动拦截停牌、涨停买入、跌停卖出，日志会提示 |
| 防御仓 | 511880 为货币 ETF，波动极低，用于弱势期资金避险 |
| 持仓认领 | 实盘启动默认从账户认领已有持仓（`claim_existing_positions`），避免与策略簿记冲突 |

### 4.3 多策略同时部署（可选）

若需与其它策略共用同一账户运行，使用多实例模式。参考 [docs/usage.md](../docs/usage.md#5-运行多策略实例)：

```json
{
  "instances": [
    {
      "instance_id": "qixing_gaozhao_real",
      "strategy_name": "qixing_gaozhao",
      "mode": "real",
      "account_id": "<资金账号>",
      "initial_capital": 200000,
      "claim_existing_positions": true,
      "kwargs": {}
    }
  ]
}
```

```bash
.venv\Scripts\python.exe -u main.py --mode instances --instances config\instances.json --debug
```

---

## 5. 运行监控与运维

### 5.1 日志

- 日志自动写入 `LOG/` 目录（按策略名命名，`*.log`，已加入 .gitignore 不上传）
- 查看运行进度：
  ```powershell
  Get-Content LOG\qixing_gaozhao.log -Tail 50 -Wait
  ```

### 5.2 常用运维操作

| 操作 | 说明 |
|------|------|
| 停止程序 | Ctrl+C，安全退出（撤单/保存状态由框架处理） |
| 每日收盘 | 程序 15:05 自动退出；配合 Windows 计划任务可次日自动启动 |
| 清理旧日志 | 运行 `clean_old_logs.bat` |

### 5.3 关注指标

| 指标 | 关注点 |
|------|--------|
| 信号质量 | 每日「动量排名前5」是否与强势板块一致 |
| 成交回报 | 有无频繁撤单/废单（滑点过大或流动性不足信号） |
| 止损频率 | 止损太频繁（> 每月 2-3 次）需回查行情或参数 |
| 防御仓占比 | 长期大量持有 511880 = 市场无趋势，属策略预期 |

---

## 6. 策略参数调优（上线后按需）

参数位于 `qixing_gaozhao_etf_strategy.py` 的 `params` 元组。**实盘调参必须先在回测+模拟盘验证**。

常用可调项：

| 参数 | 默认 | 调参方向 |
|------|------|---------|
| `holdings_num` | 5 | 越大越分散、回撤越小，收益弹性越低 |
| `stop_loss` | 0.92 | 调低（如 0.90）止损更晚触发，抗震荡持仓；调高更保守 |
| `rebalance_threshold` | 0.05 | 调大减少交易频率（省手续费），调小更贴近目标仓位 |
| `max_score_threshold` | 5.0 | 过滤过高斜率标的，防止追高（原版核心风控之一） |

运行时覆盖（不改代码）示例：

```bash
.venv\Scripts\python.exe -u main.py --mode sim --strategy qixing_gaozhao ^
    --qmt-path "D:\国金QMT交易端模拟New\userdata_mini" --strategy-params "stop_loss=0.90,holdings_num=8"
```

---

## 7. 与原始资料的已知差异（部署前必读）

| # | 原版（聚宽） | 本项目实盘表现 | 影响 |
|---|-------------|--------------|------|
| 1 | QDII 用基金净值算动量 | 实盘用市场收盘价（QMT 行情源无净值接口） | QDII 场内溢价会轻微扰动动量信号，可接受 |
| 2 | 盘中每分钟止损 | 每个交易日收盘 K 线检查一次 | 盘中急跌当天才会反映，止损粒度变粗 |
| 3 | 14:00/14:01 集中交易 | 每日 K 线闭合触发调仓 | 成交时点不同，信号价以当日收盘为准 |
| 4 | 0.1% 滑点 | 实盘限价单真实成交，回测未显式配置滑点 | 实盘成交价可能偏离信号价，需模拟盘校准 |

> 若希望保留「盘中止损」风控，可在未来版本中接入分钟级数据
> （`--period 1m`）或将止损改为日线被动风控（跌破均线离场），
> 改动前先在回测验证。

---

## 8. 风险提示（务必阅读）

1. **实盘风险**：ETF 动量轮动策略在单边下跌/长期震荡市场仍可能出现明显回撤（回测 5.7 年最大回撤 -20.6%），实盘请控制单策略资金占比。
2. **信号偏差**：日线策略实盘按当日收盘信号、收盘价成交，与回测一致；但盘中价格变动可能导致实际成交价劣于信号价。
3. **QMT 依赖**：QMT 客户端退出/断连时策略停止运行，请确保部署机器稳定（建议定时任务开机自启 QMT + 策略）。
4. **模拟先行**：**未经至少 1-4 周模拟盘验证，不要直接上实盘**。
5. **止损粗粒度**：当前实现每日检查止损，盘中急跌无法即时离场，大波动日请人工关注持仓。

---

## 9. 相关文件

| 文件 | 说明 |
|------|------|
| `qixing_gaozhao_etf_strategy.py` | 策略实现（注册名 `qixing_gaozhao`） |
| `config.py` | ETF 池 / QDII 集合 / 防御 ETF |
| `run_qixing_gaozhao_backtest.py` | 回测脚本（验证部署用） |
| `readme.md` | 策略说明与回测结果 |
| `聚宽策略源代码.py` | 原始聚宽参考代码 |
| `../docs/usage.md` | 框架级模拟/实盘/多实例使用文档 |
